{
  "name": "webdevfinalproject",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  
  "scripts": {
    "dev": "nodemon --watch server server/index.js",
    "start": "node index.js",
    "build": "npm i -ws && npm run build -ws"
  },
  "workspaces": [
    "./client"
  ],
  "dependencies": {
    "express": "^4.18.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}